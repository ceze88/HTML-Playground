import { InputFormatterPipe } from './input-formatter.pipe';

describe('InputFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new InputFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
